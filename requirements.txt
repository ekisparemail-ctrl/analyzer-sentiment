pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<1
confluent-kafka>=2.4,<3
yt-dlp>=2024.8.6
faster-whisper>=1.0,<2
pillow>=10.3,<11
# Already present transitively today; made a direct dependency now that
# nothing else in the tree will pull it in once torch/transformers are gone
# -- used directly for the grayscale keyframe-difference scoring.
numpy>=1.26,<3
