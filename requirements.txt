pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<1
confluent-kafka>=2.4,<3
yt-dlp>=2024.8.6
faster-whisper>=1.0,<2
# torch>=2.6 required: fixes CVE-2025-32434 (torch.load RCE, still exploitable
# under weights_only=True on earlier versions -- transformers' from_pretrained
# uses torch.load internally for non-safetensors checkpoints).
torch>=2.6,<3
transformers>=4.45,<5
pillow>=10.3,<11
